# Dependency directories (never commit these)
node_modules/
jspm_packages/
web_modules/

# Local environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Build outputs, logs, and caches
dist/
build/
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache
.node_repl_history

# Test coverage reports
coverage/
.nyc_output/

# Operating system files
.DS_Store
Thumbs.db

# Uploads directory (keep .gitkeep to preserve the directory structure)
uploads/*
!uploads/.gitkeep
